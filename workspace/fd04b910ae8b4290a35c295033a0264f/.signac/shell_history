jobs = list(jobs)
radii = list(sorted([job.sp.radii for job in jobs]))
radii = list(sorted([job.sp.radius for job in jobs]))
dr = min([radii[i+1]-radii[i] for i in range(len(radii)-1)])
dr
dr = round(min([radii[i+1]-radii[i] for i in range(len(radii)-1)]), 3)
dr
import numpy as np
new_radii = list([round(x, 3) for x in np.arange(radii[0], radii[-1], round(dr/2., 4))])
new_radii
for r in new_radii:
	job = project.open_job({"radius": r, "dielectric": 16}).init()
